/**
 * Command to generate and trust a root certificate
 *
 * @name config
 */

'use strict';

module.exports = function(lando) {

    var trustCert = function () {
        //
    };
    // Define our task
    return {
        command: 'trust',
        describe: 'Install and trust a root certificate',
        run: function() {
        //    Generate Cert
        //    Trust Cert
        }
    };

};